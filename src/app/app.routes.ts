import { Routes } from '@angular/router';
import { HomeComponent } from './module/portifolio/pages/home/home';

export const routes: Routes = [
    {
        path: '',
        component: HomeComponent
    }
];
